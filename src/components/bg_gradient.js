import React from "react";
import styles from '../styles/Home.module.css';



const BG_Gradient = () => {
  return (
    <section>
        <div className={styles.about_sky} />
        <div className={styles.map_sky} />
    </section>
  );
};

export default BG_Gradient;
